problems
1. 16X16 grid of divs
    solution? take argument in grid function (for now will just be 16) create 16 block divs and inside of each of those divs create 16 inline divs

    html: 
    one container div in html

    javascript:

    container = document.querysecltor .container

    javascript for i < 16 i++
    block = document.createelement(div)
    block.classlist add "block"
    container.appendchild block
        for j < 16 j++
            inline = document.createelement div
            inline.addclasslist inline
            block.appendchild inline

    css:

    .block {
        display block
    }

    .inline {
        display inline
    }

